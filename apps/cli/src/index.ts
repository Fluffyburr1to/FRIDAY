#!/usr/bin/env node
/**
 * friday — the command-line client.
 *
 * ── Deliberately empty ──────────────────────────────────────────────────────
 *
 * `friday status` and `friday events tail` arrive at Milestone 1 (Heartbeat);
 * they are how the milestone is demonstrated — a live stream of events in a
 * terminal, verifiable against the audit hash chain.
 *
 * The constraint that shapes this app, and the reason it is an app rather than
 * a package: the recovery commands must work when everything else is broken.
 *
 * See: README.md · docs/01-bible/39-roadmap.md
 */

export {}
